# Codex audit — 2026-05-17 LP members area + allowlist gate

| Field | Value |
|---|---|
| **Date** | 2026-05-17 |
| **Target** | `git diff HEAD` (uncommitted) — LP members area port + uniform-allowlist auth upgrade |
| **Files reviewed** | `src/lib/gate.ts`, `src/lib/env.ts`, `src/proxy.ts`, `src/app/components/Nav.tsx`, `src/app/sitemap.ts`, `src/app/signin/page.tsx`, `src/app/zh/signin/page.tsx`, + 8 new pages: `src/app/lp/{,firm/,terms/,strategies/}page.tsx` and `src/app/zh/lp/{,firm/,terms/,strategies/}page.tsx`. Port source-of-truth: btc-data `firm/lp/*.md` (codex-cleared, see `btc-data audits/2026-05-17-lp-content.md`) |
| **Reviewer** | OpenAI codex-cli 0.125.0, `model_reasoning_effort=xhigh`, hostile-but-fair, 2 rounds |
| **Scope** | Auth-bypass · fail-closed allowlist · prefix matching · redirect-loop/open-redirect · env-at-module-load · SEO/leak hygiene · port fidelity vs cleared SSOT · EN/CN parity · build integrity |
| **Snapshot anchor** | N/A — auth/content change, no live data |
| **Tokens** | round 1 ≈ 25,236; round 2 ≈ 25,898 (xhigh) |
| **Build** | `next build` green both rounds — TypeScript clean, 28 pages, all `/lp` + `/zh/lp` render `ƒ (Dynamic)`, `sitemap.xml` `○ (Static)` and does not include `/lp` |
| **Verdict** | **SHIP — 0 CRITICAL.** Round 1 FIX-THEN-SHIP (security S1–S5 all cleared first pass; 5 S6 port-fidelity findings). Round 2 after faithful re-port: **SHIP, no CRITICAL, F1–F9 all FIXED.** |

---

## Findings table

| ID | Severity | Surface | File | R1 | R2 | Finding / Resolution |
|---|---|---|---|---|---|---|
| F1 | MED | S6 port | `lp/firm`, `zh/lp/firm` | open | FIXED | "Vision — North Star" / "愿景 — 北极星目标" qualifier was dropped → restored EN+CN. |
| F2 | MED | S6 port | `lp/firm` | open | FIXED | EN "(the primary book)" appositive dropped → restored. |
| F3 | MED | S6 port | `lp/firm`, `zh/lp/firm` | open | FIXED | Scope will-not parentheticals "(capacity conflict)"/"(not our edge)" / "（与容量冲突）"/"（不是我们的边际所在）" dropped → restored EN+CN. |
| F4 | HIGH | S6 port | `lp/terms`, `zh/lp/terms` | open | FIXED | Explicit per-category `[PENDING — …]` markers had been paraphrased into prose (weakening the all-pending control on the legal page) → restored verbatim EN+CN. |
| F5 | HIGH | S6 port | `lp/terms`, `zh/lp/terms` | open | FIXED | EN "prime/custody" had become "prime and custody" → restored. CN matches SSOT CN ("审计机构、托管", no "prime" term) — codex confirmed this is a pre-existing, already-cleared SSOT EN/CN asymmetry, **not** a port defect. |
| F6 | — | S1 bypass | all 8 LP pages | clear | FIXED | Every page exports `dynamic='force-dynamic'` and calls `await requireViewer(<correct path>)` as the first statement of the async default export. No gate bypass. |
| F7 | — | S5 hygiene | all 8 LP pages | clear | FIXED | All carry `robots` noindex/nofollow/nocache + `googleBot` noindex/noimageindex; none added to `sitemap.ts` PAGES; proxy matcher unchanged. |
| F8 | — | S2/S4 authz | `gate.ts`, `env.ts` | clear | FIXED | Allowlist tier server-enforced; `/lp`,`/zh/lp`,`/deck` + slash-descendants only (`/lpx` does not match); `/desk/*` stays Phase-1 any-auth; **fail-closed** (unset/empty `LP_ALLOWLIST` ⇒ empty set ⇒ deny); email lowercased + null-safe; `LP_ALLOWLIST` read inside `lpAllowlist()` at request time (no module-scope read); `assertStrongAuthSecret()` still runs first in `requireViewer`. |
| F9 | — | S3 redirect | `proxy.ts`, signin EN+ZH | clear | FIXED | `/signin` never gated (proxy GATED + gate exclude it) ⇒ no redirect loop; `from` is `safeRelativePath` same-origin-relative only, `&denied=1` is a fixed literal; signed-in-but-unallowlisted gets an informational denied message, not a loop. |

---

## Cleared items (hostile review, no defect)

- **No auth bypass.** All 8 gated pages enforce `requireViewer` first-statement + `force-dynamic`; the desk-note-runbook landmine (templated pages silently lacking the gate) does not occur here.
- **Fail-closed by construction.** Empty/unset `LP_ALLOWLIST` denies the entire allowlist tier — a missing env cannot mean "open".
- **No open-redirect / no module-scope secret read** (carried codex constraints from `audits/2026-05-16-deck-sharing-*`): both upheld; `next build` is green secretless.
- **`/deck` correctly joined the allowlist tier** purely via the `gate.ts` `ALLOWLIST_PREFIXES` change — its existing `requireViewer('/deck')` now enforces allowlist with no route edit.
- **Port fidelity vs SSOT** restored verbatim round 2; no reintroduced edge mechanism, no fabricated legal fact, signature phrase "long enough to be measured" intact, bilingual disclaimer present, no EN/CN drift beyond the already-cleared SSOT asymmetry.

---

## Open items / follow-up (NOT blockers; require human action before/at deploy)

1. **`LP_ALLOWLIST` must be set in Vercel (Production + Preview) before deploy** — until then, fail-closed means `/lp/*`, `/zh/lp/*`, AND `/deck` are inaccessible to *everyone* (including the user). Comma/space/newline-separated emails, case-insensitive.
2. **`/deck` live-behaviour change (user-approved):** previously any authenticated Google user could view it; now allowlist-only. Expected and intended; flagged so it is not a surprise post-deploy.
3. **Deploy gate:** uncommitted. Per `project_hysres_deck_sharing_decision` + `firm/lp/README.md` pipeline — commit/push/deploy only on explicit user go-ahead. `.claude/` must never be staged.
4. **UI not browser-verified** (honest limitation): gated + fail-closed routes cannot complete a real Google OAuth round-trip in a local preview. Proof of correctness = `next build` green + this hostile DIFF audit, mirroring the desk-note verification convention.
